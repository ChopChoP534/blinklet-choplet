{
  "name": "blinklet",
  "version": "1.0.0",
  "private": true,
  "description": "No-code builder for Solana Blinks",
  "license": "MIT",
  "keywords": [
    "solana",
    "blinks",
    "solana-actions",
    "nextjs",
    "typescript"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "concurrently -n backend,frontend -c blue,magenta \"npm --prefix backend start\" \"npm --prefix frontend start\"",
    "lint": "npm --prefix frontend run lint",
    "type-check": "npm --prefix backend run type-check && npm --prefix frontend run type-check",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "format": "prettier --write \"backend/src/**/*.ts\" \"frontend/src/**/*.{ts,tsx,css}\" \"frontend/*.{ts,mjs}\" \"*.json\"",
    "format:check": "prettier --check \"backend/src/**/*.ts\" \"frontend/src/**/*.{ts,tsx,css}\" \"frontend/*.{ts,mjs}\" \"*.json\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.9.6"
  }
}
